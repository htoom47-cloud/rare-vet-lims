{
  "name": "rare-vet-lims",
  "version": "1.0.0",
  "private": true,
  "description": "AL NAWADER VETERINARY CARE CENTER LIMS — cloud deployment",
  "scripts": {
    "dev:staff": "cd frontend && npm run dev",
    "dev:portal": "cd frontend-portal && npm run dev",
    "build:cloud": "node scripts/build-cloud.js",
    "build:portal": "cd frontend-portal && npm ci --include=dev && VITE_API_URL=/api npm run build",
    "start:cloud": "cd backend && node src/scripts/cloud-start.js",
    "seed:cloud": "cd backend && node src/scripts/seed.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
