import { HorseIcon } from './AnimalTypeIcon';

/** Navigation / KPI icon for animals — lab line art, not a generic paw. */
export default function AnimalsNavIcon({ size = 24, className }) {
  return <HorseIcon size={size} className={className} />;
}
