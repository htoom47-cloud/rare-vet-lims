import { CamelIcon } from './AnimalTypeIcon';

/** Herd / breeder nav icon — camel line art, not a paw. */
export default function HerdNavIcon({ size = 24, className }) {
  return <CamelIcon size={size} className={className} />;
}
