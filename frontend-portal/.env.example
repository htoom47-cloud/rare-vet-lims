# API base URL (production: your backend + /api)
# Local dev uses Vite proxy — leave empty or use /api
VITE_API_URL=/api

# Official portal URL (informational — set on server / Render)
# PORTAL_APP_URL=https://portal.rarevetcare.com
