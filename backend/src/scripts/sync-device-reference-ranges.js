/**
 * Norma device reference range sync — DISABLED.
 * Ranges are managed manually in the LIMS UI.
 */
require('dotenv').config();

console.log('\nDevice reference range sync is disabled — manage ranges manually in LIMS.\n');
process.exit(0);
